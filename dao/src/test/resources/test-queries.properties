file.getFileById=SELECT * \
                 FROM file \
                 WHERE id = :id
folder.getFolderByFolderId=SELECT * \
                           FROM folder \
                           WHERE id = :id
role.getRolesByUserId=SELECT role_id \
                      FROM user_role \
                      WHERE user_id = :user_id